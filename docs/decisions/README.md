# Research decisions

These files are the compact verdicts for major research checkpoints. Read these when you want the conclusion; read `../experiments/` when you want the full protocol, raw-history narrative, methodology corrections and reruns.

## Chronology

| Checkpoint | Decision / significance |
|---|---|
| [`SCALE_UP_DECISION.md`](SCALE_UP_DECISION.md) | Initial typed-domain / Commerce IR / physical-index prototype: proceed to real-data falsification. |
| [`ROUND1_DECISION_TREE.md`](ROUND1_DECISION_TREE.md) | Real ESCI + Solr evidence: **narrow the product** rather than rebuild a general search engine. |
| [`PHASE2_DECISION.md`](PHASE2_DECISION.md) | Whole-engine replacement thesis: **STOP**. Mature lexical ranking remains delegated. |
| [`PHASE3_DECISION.md`](PHASE3_DECISION.md) | Safe admission frontier: narrow support for selective structural traffic. |
| [`PHASE4_DECISION.md`](PHASE4_DECISION.md) | Offline learned implication rules: useful only with strict validation/promotion gates. |
| [`PHASE5_DECISION.md`](PHASE5_DECISION.md) | Browse/PLP operators expose real cardinality-dependent win/loss regions. |
| [`PHASE6A_DECISION.md`](PHASE6A_DECISION.md) | WANDS cross-dataset validation. |
| [`PHASE6B_DECISION.md`](PHASE6B_DECISION.md) | Controlled WANDS scale ladder; candidate size and attribute complexity characterized. |
| [`PHASE6C_DECISION.md`](PHASE6C_DECISION.md) | Direct Lucene baseline corrected the naive facet-algorithm interpretation. |
| [`PHASE6D_DECISION.md`](PHASE6D_DECISION.md) | Ordinal faceting closes the earlier color-facet crossover; also finds its own small-candidate typed-ID limit. |
| [`PHASE6E_DECISION.md`](PHASE6E_DECISION.md) | Embedded Elasticsearch/OpenSearch baselines become runnable; Havenask remains blocked in the measured environment. |
| [`PHASE7_DECISION.md`](PHASE7_DECISION.md) | Multi-tenant pooling economics: promising, with concrete rebuild/backend isolation gaps. |
| [`PHASE8_DECISION.md`](PHASE8_DECISION.md) | Correlated burst tests: pure query bursts hold; known isolation gaps become more reliable/worse under burst. |
| [`PHASE8_FEASIBILITY.md`](PHASE8_FEASIBILITY.md) | What Phase 8 could and could not validly test in the available environment. |
| [`PHASE9_DECISION.md`](PHASE9_DECISION.md) | Integrated Phase 9 evidence and corrected query-resolution baseline. |
| [`ISSUE38_DECISION.md`](ISSUE38_DECISION.md) | Dynamic compiled merchant schema: hot-path overhead can be removed; unseen/mixed synthetic generalization succeeds within its stated scope. |
| [`ISSUE42_DECISION.md`](ISSUE42_DECISION.md) | R2 residual policy and R3 identifier primitive GO; R1 and model-assisted E2b remain REVISE. |
| [`ISSUE45_DECISION.md`](ISSUE45_DECISION.md) | Deterministic semantic canonicalization substantially reduces LLM instability but remains **REVISE** under the stricter single-proposal reading. |
| [`ISSUE43_DECISION.md`](ISSUE43_DECISION.md) | Phase 9 reproducibility re-audit: published numbers **CONFIRMED** byte-identical against the Tantivy determinism fix; unrelated Solr-JVM-warmup confound found and disclosed as a new open thread. |
| [`ISSUE55_H3_DECISION.md`](ISSUE55_H3_DECISION.md) | Variant-scoped conjunction correctness **CONFIRMED** on real Product/Variant data (Magento configurable products) for the first time, closing the external-validity gap `ISSUE47_DECISION.md` named; FastPath only, disclosed scope boundary. |
| [`ISSUE51_DECISION.md`](ISSUE51_DECISION.md) | Precomputed corroboration registry (Treatment E): correctness-preserving, but **REVISE** at R1's own 5-product fixture; a disclosed scaling diagnostic shows a 492x asymptotic advantage over the query-time scan at realistic catalog sizes, naming a concrete next step. |
| [`ISSUE55_RANK_SCALING_DECISION.md`](ISSUE55_RANK_SCALING_DECISION.md) | `execute_ranked`'s full sort replaced with a proven-equivalent partial top-K selection (adopted); **REFINE** — real but modest gain, not the dominant driver of P9-E06's native-vs-Solr gap on real WANDS data, which localizes instead to Solr JVM variance and a newly found `score_text_relevance` tokenization cost. |
| [`ISSUE55_TEXT_TOKEN_CACHE_DECISION.md`](ISSUE55_TEXT_TOKEN_CACHE_DECISION.md) | `score_text_relevance`'s per-query tokenization precomputed at index-build time (adopted, 43-59% synthetic cost reduction); **KEEP, and reverses Phase 9's H3 verdict** — on the same real WANDS data that originally found native slower (0.42x-0.60x), native is now consistently 4.6x-8.2x **faster** than Solr-restricted. `PHASE9_DECISION.md` corrected via dated addendum. Scope later narrowed by a dated addendum (see next row): regresses ~1.7x-2.3x on queries with empty residual-lexical/preferences. |
| [`ISSUE55_WHOLE_WORKLOAD_DECISION.md`](ISSUE55_WHOLE_WORKLOAD_DECISION.md) | Traffic-weighted re-measurement of the H3 fixes on real `p9_e02` end-to-end traffic: **REFINE** — the isolated 4.6x-8.2x gain does not hold uniformly; root-caused to an unconditional per-candidate `HashMap` lookup the fix added ahead of the old code's zero-work short-circuit for empty-residual/empty-preference queries, confirmed by three independent measurements (an initial cache-locality theory was tested directly and falsified). Hybrid-traffic dilution and a `select_nth_unstable_by` tie-heavy regression were also tested directly and ruled out. Fix scope narrowed, not reverted; hoisting the short-circuit is named as the next experiment. |
| [`ISSUE55_EMPTY_RESIDUAL_FIX_DECISION.md`](ISSUE55_EMPTY_RESIDUAL_FIX_DECISION.md) | The named fix (hoist the emptiness check before the `HashMap` lookup): **KEEP** — regression eliminated (back to pre-Issue-55 baseline on the two known queries), confirmed-win path gains an *additional* 2.5-8.2% from the same reorder, no correctness change. Whole-workload `structural_routed` aggregate returns to ~2.48x, essentially the original pre-Issue-55 baseline — explained, not a disappointment: independently confirmed to be dominated by `index.execute()`/`lookup_variant` candidate-materialization cost no Issue #55 fix targeted, not by scoring/sorting (now measurably cheap). Closes the text-token-cache checkpoint's regression addendum; names a planner-policy follow-up (should 100%-of-catalog candidate sets route to `FastPath` at all) as still open. |
| [`ISSUE55_AMBIGUOUS_ROUTING_DECISION.md`](ISSUE55_AMBIGUOUS_ROUTING_DECISION.md) | Investigated whether the planner discards preserved query ambiguity (`plan()` never reads `query.ambiguous`) — found a real, mechanistically-confirmed local effect (n=4 real WANDS queries, -65.51% NDCG gap), but **REJECT/SUPERSEDED** as a new finding: an adversarial-review workflow found this exact question was already answered at ~100x scale by `PHASE3_DECISION.md` ("ambiguity supplies no ranking signal") and already partially solved by a sibling, uncomposed module (`admission::admit`'s `RejectReason::Ambiguous`). Whole-workload impact negligible (~0.45pp). Folded into `ISSUE55_EMPTY_RESIDUAL_FIX_DECISION.md`'s existing follow-up and a `PHASE3_DECISION.md` addendum rather than treated as new work. Preserved as a positive example of adversarial review catching an overclaim before it was recorded as a decision. |
| [`ISSUE51_FULLGATE_SCALE_DECISION.md`](ISSUE51_FULLGATE_SCALE_DECISION.md) | Reran R1/Issue #42's full `<=5%` `execute_planned` overhead gate at realistic (~43,000-product) catalog scale, closing `ISSUE51_DECISION.md`'s own named next step. **REVISE, confirmed not resolved**: Treatment E still fails the bar (14.0%-16.7% across 3 runs, essentially unchanged from R1's original N=5 measurement) — the registry mechanism's own asymptotic advantage is real but was never the dominant cost; a per-row breakdown found the actual driver is the `Punt` fallback's lexical-delegate query cost for genuinely ambiguous, uncorroborated queries, untouched by Issue #51's optimization. Two real methodology defects (decoy attribute pollution collapsing NDCG; a decoy price collision turning a regression-guard row into an accidental 97%-of-latency full-catalog scan) were found and fixed before trusting any number. |
| [`ISSUE55_PRODUCT_CLASS_INGESTION_DECISION.md`](ISSUE55_PRODUCT_CLASS_INGESTION_DECISION.md) | Investigated why native's structural candidate sets miss ~55% of judged-relevant WANDS documents (`p9_e04`'s own coverage diagnostic) despite native's ranking signal itself not being worse than Solr's on an identical candidate set. Found and fixed a real WANDS ingestion gap (11.86% of the catalog had null or pipe-delimited `product_class`, mapping to an unmatchable sentinel or a garbled compound string). **KEEP the fix** (correct, harmless, recovers real data) **but REJECT it as an answer to the coverage gap**: candidate-set recall moved only +1.02pp (below the preregistered >=5pp bar) and `p9_e02`'s own NDCG numbers are byte-identical before/after. The dominant remaining mechanism, confirmed via unchanged qualitative examples, is category-hierarchy/synonym mismatch (e.g. a real "Kids Beds" product not matching a query resolving to "beds") — named, not implemented, as the next candidate follow-up (a `BrandAny`-style `ProductTypeAny` synonym/hierarchy mechanism). |
| [`ISSUE55_PRODUCT_TYPE_HYPONYM_DECISION.md`](ISSUE55_PRODUCT_TYPE_HYPONYM_DECISION.md) | Implemented the named follow-up: a whole-word `ProductTypeAny` hyponym-expansion mechanism (structurally identical to `BrandAny`). Cleared both preregistered gates by the largest margin of any checkpoint this session (+24.06pp candidate-set recall; ranking quality and speed still held) — then **REJECT**ed anyway: a dedicated real-vocabulary audit (`p9_e08`, run to actually execute the protocol's own "no wrong-family regression" gate rather than infer it from aggregate NDCG) found confirmed cross-family false positives on real WANDS data (e.g. "beds" admitting pet "cat beds"; "candles" admitting "scented oils & diffusers" via an unrelated ancestor breadcrumb). Aggregate NDCG did not surface this — a generalizable methodology finding. Production wiring reverted to safe per-id `ProductType` matching; the tested `ProductTypeAny` infrastructure, the hyponym-group pure function, and the audit tool are kept for a future, hierarchy-aware (not bag-of-words) redesign, named but not implemented. |
| [`ISSUE51_PUNT_COST_FLOOR_DECISION.md`](ISSUE51_PUNT_COST_FLOOR_DECISION.md) | Closes `ISSUE51_FULLGATE_SCALE_DECISION.md`'s own named follow-up: is the `Punt`-path delegate-query cost blocking Treatment D/E's `<=5%` overhead gate a fixable inefficiency? **No** — isolating the exact two operations involved (`identifier_lookup` + `BitmapTantivyDelegate::search`), called directly with no surrounding treatment machinery, reproduces 94.3%-98.8% of the measured cost across two runs. A supplementary measurement found a genuine text match costs 45.6x more than the fixture's zero-hit case, meaning real traffic would cost even more. The `<=5%`-vs-Treatment-A bar is a property of the gate's own framing (paying for a real search when structural evidence cannot disambiguate is what correctness costs), not a defect in any mechanism this session built — closes the Issue #51 overhead-gate thread definitively; redefining what the gate measures is named as the one remaining direction, not implemented. |
| [`ISSUE35_ESCI_ELECTRONICS_DECISION.md`](ISSUE35_ESCI_ELECTRONICS_DECISION.md) | First real, disclosed slice of Issue #35's unseen-vertical generalization epic: does the existing discovery/serving pipeline handle a genuinely new commerce vertical (real Amazon electronics/components listings, a filtered ESCI slice) with zero `commerce-core` changes and zero hand-authored vertical ontology? **H0 confirmed** — ran unmodified with no crash/edit, zero wrong-family `Brand` violations across 59 brand-constrained queries, and native NDCG@10 (0.3041) beat a real Solr baseline (0.2792, +8.93%), spot-checked as a fair comparison. Routing is 90.2% `Punt` (no product-type/category signal exists in this vertical's data at all) — disclosed as this vertical's own specialization-boundary finding, not a defect, per Issue #35's own allowance that a vertical can be "not worth specializing." One genuine, rare precision risk found by direct investigation: a real product's brand string `"IS"` collides with the common word "is," spuriously triggering a (zero-result, not wrong-result) `Brand` constraint on an unrelated query — quantified at 1/1,079 discovered brands. Explicitly scoped as one slice of Issue #35, not the full epic. **Dated correction**: the originally-named follow-up (extend `min_enum_frequency`-style trust filtering to brand discovery) is withdrawn — `docs/experiments/PHASE2_LOG.md`'s own P2-E05–P2-E10 already found, across three independent mechanisms on the real full ESCI catalog, that trusting *more* brand strings as hard filters (however intelligently classified) costs more real recall than it gains ("CANONICALIZATION FRONTIER IS FUNDAMENTAL"); a bespoke stopword blocklist would re-litigate an already-closed question. |
| [`ISSUE55_HYPONYM_LEAF_ONLY_DECISION.md`](ISSUE55_HYPONYM_LEAF_ONLY_DECISION.md) | Implements checkpoint 11's named follow-up: restrict `ProductTypeAny`'s whole-word hyponym comparison to each product-type name's trailing path segment (leaf) instead of the full ancestor-breadcrumb path. **KEEP** (supersedes checkpoint 11's REJECT for the corrected mechanism) — a re-audit confirms all three named cross-family false positives ("candles"→diffusers, "hot tubs"→saunas, "bed accessories"→shower curtains) are gone, the flagship "recliners" win survives, and 94.1% of the original +24.06pp recall improvement is retained; H1 (ranking quality) still holds, H3 (speed) is honestly reported as borderline across 3 runs (median ~2.04x, down from 2.54x) rather than rounded up. The disclosed "beds"→pet-products residual risk remains, exactly as predicted, since it involves no path structure for leaf-restriction to act on. **Major incidental finding**: re-running `p9_e02` shows `structural_routed` NDCG turning positive for the first time this entire session (+5.37% vs. Solr, was -25.05%) — at a disclosed latency cost (ratio drops to 1.19x) that keeps the overall verdict at REVISE, not GO; named as a sharp new question (recover latency margin, or reconsider the `>=2x` bar now that relevance has improved) rather than resolved here. |
| [`ISSUE35_ESCI_AUTOMOTIVE_DECISION.md`](ISSUE35_ESCI_AUTOMOTIVE_DECISION.md) | Second, materially different unseen-vertical slice for Issue #35 (real Amazon automotive parts/accessories via ESCI, 1,056 products/600 queries) — reusing the electronics checkpoint's measurement code (extracted into a shared function, verified byte-identical before reuse). **H0 CONFIRMED again**: zero `commerce-core` changes, zero wrong-family `Brand` violations across 37 queries, native NDCG@10 within the `<=15%` bar (-2.55% this time, vs. +8.93% for electronics — reporting both directions honestly). The rare brand/stopword collision found in the electronics slice does **not** recur here (0/502 discovered brands), consistent with it being an isolated risk rather than a systemic one. Two of Issue #35's named "≥3 materially different verticals" are now done. |
| [`ISSUE35_ESCI_BEAUTY_DECISION.md`](ISSUE35_ESCI_BEAUTY_DECISION.md) | Third, materially different unseen-vertical slice (real Amazon beauty/personal-care listings via ESCI, 2,093 products/600 queries), **completing Issue #35's own named "≥3 materially different verticals" Workstream D bar**. **H0 CONFIRMED a third time**: zero `commerce-core` changes, zero wrong-family `Brand` violations across 46 queries, native NDCG@10 within the `<=15%` bar (-1.38% — electronics +8.93%, automotive -2.55%, beauty -1.38%: near-parity in three different directions/magnitudes, evidence against a systematic measurement bias). Zero brand/stopword collisions (0/1,231), a third consistent data point. One new, disclosed quirk: a query resolved to a `Brand` `AND` `color="Lotion"` conjunction (zero hits) because this catalog overloads `product_color` for form-factor descriptors, not just literal color — a safe null result, not a wrong one. |
| [`ISSUE35_SOLR_HARNESS_HARDENING_DECISION.md`](ISSUE35_SOLR_HARNESS_HARDENING_DECISION.md) | Research-hygiene audit (Issue #55 P0.2): the Issue #35 evaluator's `solr_search` converted Solr transport errors, JSON-parse failures, and `responseHeader.status != 0` query errors into the same empty result list an unconditional `unwrap_or(0.0)` would then score as a legitimate Solr NDCG=0 loss — indistinguishable from a real zero-result query. **KEEP the fix**: `solr_search` now returns a 3-way `SolrLookup` outcome; the native-vs-Solr comparison is a paired set that excludes (never zero-scores) a failed query; any transport/parse failure makes the run print every failing query and `exit(1)` before printing any relevance numbers (this Solr core is same-host/locally-controlled, so a failure is a real infra defect, not expected flakiness) — preregistered, not a threshold moved after the fact. Rerunning all three existing checkpoints (electronics/automotive/beauty) against the project's own local Solr 9.10.1 install reproduced their originally recorded NDCG numbers byte-for-byte (0 Solr errors in all three) — **no historical conclusion changes**; the bug existed throughout those runs but never fired because Solr was reachable and healthy each time, confirmed directly rather than assumed. A fourth adversarial rerun against a deliberately unreachable Solr URL confirmed the fix is not a no-op: `exit 1` and zero fabricated relevance numbers, where the unfixed harness would have silently printed a fabricated native-vs-Solr win. |
| [`ISSUE55_PAIRED_COMPARATOR_DECISION.md`](ISSUE55_PAIRED_COMPARATOR_DECISION.md) | Priority 1A: resolves checkpoint 14's own disclosed anomaly (Solr's NDCG moved even though the native treatment should not itself change Solr's ranking). A frozen paired-comparator experiment confirms the before/after `structural_routed` cohort was the literal same 21 query IDs (not just the same count) and that this local Solr instance is fully deterministic (0 stdev across repeated identical-text calls) — ruling out cohort drift and Solr nondeterminism. Finds the real cause instead: `p9_e02_wands_physical_advantage.rs`'s Solr-query builder had no match arm for `StructuralConstraint::ProductTypeAny` (a constraint kind checkpoint 14 itself first made reachable), so Solr silently received no product-type filter at all for the treatment's own cohort — an asymmetric comparator bug, not Solr drift. **Fixed the missing arm, reran `p9_e02` independently, and it reproduces this experiment's own corrected number exactly**: `structural_routed` relative gap is `-25.05% -> -20.49%` under a fair comparator, not checkpoint 14's originally reported `+5.37%` — a real, substantial narrowing, but not a native win. A cleaner, more durable finding survives: splitting by routing shows FastPath native materially worse than Solr (-66.11%, n=7) while Hybrid is roughly at parity (+3.02%, n=14) — confirms the governing task's own named hypothesis that structural anchors + lexical residual (Hybrid) carry real value while forcing complete structural execution (FastPath) may not. Reproduced 3x, byte-identical. |
| [`ISSUE55_ROUTING_OUTCOME_REPLICATION_DECISION.md`](ISSUE55_ROUTING_OUTCOME_REPLICATION_DECISION.md) | Tests whether the WANDS FastPath-worse/Hybrid-near-parity split above replicates on the three independent ESCI verticals. Found and fixed a second, mirror-image occurrence of Priority 1A's own defect class: `issue35-eval`'s Solr comparator sent no `Brand`/`color` `fq` at all, letting Solr answer an easier, unrestricted question than native's `Hybrid`/`FastPath` execution actually enforces — verified with a new wire-level regression test, moving two verticals' Hybrid gaps by 25-27pp (no prior aggregate KEEP verdict affected; <0.5pp aggregate movement). **KEEP the fix; REJECT the replication itself**: even under the fair comparator, none of the three ESCI verticals reproduce WANDS's pattern — Hybrid is worse than Solr everywhere (not near-parity), and FastPath is inconsistent (automotive reverses WANDS's own direction; beauty near-parity; electronics has no FastPath-routed evaluated queries). Recorded as a first-class negative result scoped to a secondary, exploratory disaggregation, not the primary preregistered aggregate metric each vertical's own H0 verdict rests on. Automotive's still-large post-fix Hybrid gap (-38.75%) is named as the one open follow-up thread. |
| [`ISSUE55_HYBRID_ZERO_HIT_MECHANISM_DECISION.md`](ISSUE55_HYBRID_ZERO_HIT_MECHANISM_DECISION.md) | Investigates that open thread. A qualitative per-query probe found native sometimes returns literally **zero** hits for a Brand-constrained Hybrid query with a real relevant product in the catalog (e.g. `"castrol 10w30"`: native NDCG=0.0, Solr NDCG=0.4791 with a real Exact match). Confirms two compounding, code-path-verified mechanisms: the delegate's Tantivy tokenizer splits on punctuation (`"10W-30"` indexes as two tokens, never matching the concatenated residual token `"10w30"`), and Issue #42 R2's residual-lexical fallback (designed for exactly this situation) can never fire on any ESCI vertical — the harness never wires up a `residual_policy`, and even if it did, the fallback's `corroborating_product_type` precondition can never be satisfied since no ESCI vertical registers product types at all. Quantified across all three verticals: automotive 14/32 (43.75%) zero-hit Hybrid queries, 4/32 (12.5%) confirmed "recoverable misses"; electronics 8/48 (16.7%)/1/48 (2.1%); beauty 15/38 (39.5%)/3/38 (7.9%) — automotive ranks worst on both, consistent with (not a full explanation of) its own worst Hybrid gap. **KEEP as a confirmed, disclosed, partial-explanation finding; no production change made.** A deliberately-checked nuance: relaxing R2's `ProductType`-only requirement to also accept `Brand` corroboration would be *correctness-safe* (constraints are already enforced before ranking) but is a genuine precision/confidence design question, not a free fix — named, not implemented, as the next step alongside a separate delegate-tokenization-robustness question. **Dated addendum**: that named follow-up was itself incomplete — `ResidualPolicy::classify`'s cross-`ProductType`-breadth signal (a second, independent precondition) can also never fire on any product-type-sparse catalog, so "just accept Brand too" would be a no-op; the real next step requires redesigning the breadth signal itself, revised in place. |
| [`ISSUE55_PROMOTION_GATE_FULL_SET_DECISION.md`](ISSUE55_PROMOTION_GATE_FULL_SET_DECISION.md) | Extends the Priority 2 preliminary promotion-evidence probe (top-level category overlap, tested on a hand-picked 8-pair subset, safe but 17% recall) to the full live `product_type_hyponym_groups` candidate set (149 groups, 317 pairs, exported directly from production) and adds a second evidence source (2-level category-path overlap). **REJECT both as standalone promotion rules**: the zero-false-promotion safety gate holds at full scale, but both fail the preregistered >=50% recall bar badly (30.6% top-level, 22.4% level-2). The level-2-vs-top-level comparison itself is disclosed as not well-posed (a strictly harder match can only ever promote a subset of a looser one — should have been caught while designing the protocol, not after running it). The real, quantified finding: category-hierarchy overlap can only ever adjudicate 16.1% of all pairs (51/317) — 83.9% are permanently unresolvable under this evidence family because most narrower names are ancestor-breadcrumb ingestion fallback strings with no literal `product_class` match, a coverage problem, not a precision/threshold problem. Names using the narrower name's own ancestor-breadcrumb structure directly (rather than re-deriving it through a second, frequently-failing `product_class` lookup) as the next, more targeted evidence source. |
| (no dedicated decision doc — see `ISSUE55_PROMOTION_GATE_FULL_SET_DECISION.md`'s named next step and `docs/research/artifacts/i55_promotion_gate_full_set/run2_ancestor_structure.txt`) | Implements that named next step: treats a `"/"`-containing name as direct evidence of its own ancestor path, additive to the existing `product_class` lookup. Coverage jumps from 51 to 315 resolvable pairs (94% reduction in "no evidence" cases), and **both evidence sources now clear every preregistered threshold** (zero false promotions; 67.6%/65.6% recall, both `>=50%`) — a genuine, preregistered GO on the recall question. Investigated further below before trusting the 4x jump in promotion volume. |
| [`ISSUE55_HYPONYM_REACHABILITY_AUDIT_DECISION.md`](ISSUE55_HYPONYM_REACHABILITY_AUDIT_DECISION.md) | Prompted by that 4x promotion-volume jump: a visual scan of newly-promoted pairs found what looked like a serious new cross-family false positive (`"plates"` admitting `"switch plates"`/`"burners & hot plates"`) — the first time this project had ever read through the *entire* 149-group candidate set at once, rather than rechecking previously-named groups. **Self-caught overclaim**: direct mechanical compilation of the literal query text found it produces no constraint at all, because the actual registered "broader term" is an unreachable 11-word synthesized path no shopper would type. A systematic reachability sweep of all 149 groups found only 79 (53%) are reachable via their own literal text at all; of those, a full read confirmed **exactly one** cross-family false positive — the already-known `"beds"`->pet-products case — plus one low-practical-risk edge case (`"accent chests / cabinets"`->`"dartboards and cabinets"`, reachable only via an exact taxonomy-label string). **CONFIRMS (does not overturn) checkpoint 14's KEEP verdict, now on genuinely complete evidence rather than targeted rechecks; no production change.** Separately found and corrected a factual error in `ISSUE55_SEMANTIC_PROMOTION_LOG.md`, which had claimed the live candidate set was just 2 groups (it is, and always has been since checkpoint 14, 149 groups/317 pairs) — a dated addendum, not a rewrite. |
| [`ISSUE55_RESIDUAL_BREADTH_SIGNAL_DECISION.md`](ISSUE55_RESIDUAL_BREADTH_SIGNAL_DECISION.md) | Tests checkpoint 20's own revised next question: would substituting a cross-Brand-breadth axis for `ResidualPolicy::classify`'s existing cross-`ProductType`-breadth signal safely extend Issue #42 R2's residual-lexical fallback to product-type-sparse (all-ESCI-shaped) catalogs? **REJECT, before writing any commerce-core code**: this module's own documented precedent (a "second correction round" fix that removed an earlier, entity-scoped version of this exact classifier after it recovered a wrong-variant product) already establishes that scoping-axis choice is safety-critical, and `ProductType`'s correlation with genericness (it partitions by functional category) does not transfer to `Brand` (competing brands routinely share the same descriptive vocabulary). Confirmed directly against real ESCI electronics data: common, genuinely disqualifying color values have very high cross-brand breadth (`"black"` spans 259 distinct brands, `"white"` 85, `"blue"` 51) — a naive substitution would misclassify every one of these as "safe to bypass," reopening the exact wrong-variant-recovery bug class the original fix closed, just via Brand instead of ProductType. Closes this specific design idea; the underlying gap (ESCI's Hybrid-bucket zero-hit misses) remains unrecovered, and no safe alternative axis is identified. |
| [`ISSUE55_HYPONYM_PROMOTION_GATE_DECISION.md`](ISSUE55_HYPONYM_PROMOTION_GATE_DECISION.md) | Repository-owner closure directive A1: `product_type_hyponym_groups`'s syntactic candidates were installed as live `ProductTypeAny` serving routes with no validation gate at all — exactly how the confirmed cross-family false positive `"beds"` → `"cat beds"`/`"dog beds & mats"` shipped as a hard default. A new `control_plane::hyponym_promotion` module (`HyponymRelation`/`PromotedHyponyms`, reusing `implication.rs`'s Candidate/Promoted/Withdrawn lifecycle) is now the required gate: `compile_lexicon`'s public signature is unchanged, but its default promoted set is now empty, so **current production has zero active hyponym expansions** until a real adjudicated set exists (Issue #55 A2, not yet done). One existing integration test asserting the old auto-install default was found failing immediately after the fix and rewritten to demonstrate the new explicit-promotion-required behavior directly, rather than adjusted to pass. Full workspace quality gate green. |

## How to read old numbers

Decision records are intentionally not rewritten to make the research look linear. When an experiment later discovered a methodology defect, the repository preserves the old result and records the correction. A later decision may therefore supersede an earlier interpretation without deleting it.

For current project status, always prefer the root [`README.md`](../../README.md) and [`../architecture/README.md`](../architecture/README.md) over an old phase narrative.
