//! Issue #16 (Phase 4): learned semantic implication rules over Issue
//! #14's safe fast-path frontier. Shared helpers live here; each
//! experiment (`P4-Enn`) is its own binary under `src/bin/`, mirroring
//! `phase3-eval`'s own convention.
