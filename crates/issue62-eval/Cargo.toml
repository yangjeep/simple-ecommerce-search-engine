[package]
name = "issue62-eval"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Issue #62 (Infra E2) physical footprint / SKU-density scaling benchmark. Experiment code only."

[dependencies]
commerce-core = { path = "../commerce-core" }
issue61-eval = { path = "../issue61-eval" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = { version = "2", features = ["json"] }
