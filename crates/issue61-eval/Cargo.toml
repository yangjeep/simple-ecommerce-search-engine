[package]
name = "issue61-eval"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Issue #61 resource-measurement foundation for infrastructure-economics benchmarks. Experiment code only."

[dependencies]
bench-harness = { path = "../bench-harness" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
