[package]
name = "commerce-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Typed commerce domain model, IR, and retrieval primitives for the commerce-native search engine experiment (Issue #2)."

[dependencies]

[dev-dependencies]
criterion = { version = "0.5", default-features = false }
rand = "0.8"
rand_chacha = "0.3"

[[bench]]
name = "catalog_bench"
harness = false
