[package]
name = "issue57-eval"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Issue #57: frozen full-matrix E2E benchmark (native vs Solr vs Elasticsearch vs OpenSearch vs Havenask) across the recovered dataset set, per docs/experiments/FULL_MATRIX_PROTOCOL.md."

[dependencies]
comparator-eval = { path = "../comparator-eval" }
commerce-core = { path = "../commerce-core" }
phase6a-eval = { path = "../phase6a-eval" }
issue35-eval = { path = "../issue35-eval" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = { version = "2", features = ["json"] }
