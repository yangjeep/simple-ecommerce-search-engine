[package]
name = "issue38-eval"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Issue #38 E1: does compiling a dynamically-discovered feature schema into the same native physical operators (bitmap intersection) preserve most of a hard-coded specialized executor's physical advantage, versus a deliberately runtime-generic dynamic-schema executor and a Solr baseline?"

[dependencies]
bench-harness = { path = "../bench-harness" }
commerce-core = { path = "../commerce-core" }
phase6a-eval = { path = "../phase6a-eval" }
round1-eval = { path = "../round1-eval" }
roaring = "0.10"
ureq = { version = "2", features = ["json"] }
serde_json = "1"
