[package]
name = "realtime-eval"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Issue #8 experiments: does the commerce-state fast path (variant availability overlay) beat a full CatalogIndex rebuild by the margin Issue #7's revised performance thesis requires (>=5x P50/P95), on the real 1.2M-product catalog?"

[dependencies]
commerce-core = { path = "../commerce-core" }
round1-eval = { path = "../round1-eval" }
roaring = "0.10"
rand = "0.8"
rand_chacha = "0.3"
