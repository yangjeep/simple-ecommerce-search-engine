[package]
name = "issue55-eval"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Issue #55 Architecture Falsification Loop: H3 (variant-aware execution correctness) tested against a real, genuinely different-vertical Product/Variant dataset. Not part of the production evidence base; depends on commerce-core read-only."

[dependencies]
commerce-core = { path = "../commerce-core" }
comparator-eval = { path = "../comparator-eval" }
round1-eval = { path = "../round1-eval" }
phase6a-eval = { path = "../phase6a-eval" }
phase9-eval = { path = "../phase9-eval" }
issue35-eval = { path = "../issue35-eval" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = { version = "2", features = ["json"] }
